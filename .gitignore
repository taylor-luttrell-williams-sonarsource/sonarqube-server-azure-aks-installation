# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan

# Tfvars with real values (keep the .example)
terraform.tfvars.json
terraform.tfvars

# Internal project docs (not for public repo)
context.md
next-steps.md

# OS
.DS_Store
